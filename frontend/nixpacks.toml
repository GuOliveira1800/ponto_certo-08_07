[phases.setup]
cmds = ["corepack enable && corepack prepare yarn@4.12.0 --activate"]

[phases.install]
cmds = ["yarn install"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "npx serve dist -p $PORT"