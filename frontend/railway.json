{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && corepack prepare yarn@4.12.0 --activate && yarn install && yarn build"
  },
  "deploy": {
    "startCommand": "npx serve dist -p $PORT"
  }
}
